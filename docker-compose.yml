version: '3.9'
services:
  server:
    build:
      context: .
    environment:
      - SPEECH_SPEED=${speech_speed}
      - MP_WORKERS=${mp_workers}
    restart: unless-stopped
    ports:
      - '8080:8000'
    shm_size: '1gb'